{
	"icons": {
		{{#each codepoints}}
		"{{@key}}": {
			"description": "{{@key}} icon",
			"default": {
				"fontPath": "./resources/dist/icons.woff2",
				"fontCharacter": "\\\{{codepoint this}}"
			}
		{{#if @last}}
		}
		{{else}}
		},
		{{/if}}
		{{/each}}
	}
}
