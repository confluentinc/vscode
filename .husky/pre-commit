# Run linter on staged files before commit

STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(ts|js)$' | sed 's| |\\ |g')

if [ -n "$STAGED_FILES" ]; then
  npx eslint --fix $STAGED_FILES
  git update-index --again
fi
